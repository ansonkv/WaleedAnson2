<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.address.mappickup.LocationMapActivity"
    tools:showIn="@layout/activity_map_address">

    <com.google.android.gms.maps.MapView
        android:id="@+id/mapview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <TextView
        android:id="@+id/tv_change_address"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/img_marker"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="@dimen/marginProductDetails"
        android:layout_marginEnd="@dimen/marginProductDetails"
        android:background="@drawable/bg_orange_btn_curve"
        android:fontFamily="@font/open_sans_semi_bold"
        android:gravity="center"
        android:paddingStart="12dp"
        android:paddingTop="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="12dp"
        android:textColor="@android:color/white"
        android:visibility="visible" />

    <ImageView
        android:id="@+id/img_marker"
        android:layout_width="@dimen/marginProductDetails"
        android:layout_height="@dimen/marginProductDetails"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginBottom="12dp"
        android:src="@drawable/ic_location" />

    <Button
        android:id="@+id/btn_confirm_location"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_alignParentBottom="true"
        android:layout_margin="@dimen/marginProductDetails"
        android:background="@drawable/bg_orange_btn_curve"
        android:fontFamily="@font/open_sans_bold"
        android:text="@string/confirm_location"
        android:textAllCaps="false"
        android:textColor="@android:color/white"
        android:textSize="18sp" />

</RelativeLayout>